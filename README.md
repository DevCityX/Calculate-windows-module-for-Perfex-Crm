# Window Calculator module for Perfex CRM

Цей репозиторій містить модуль `window_calculator` для Perfex CRM, який:

- розраховує вартість віконної конструкції за геометрією, кількістю стулок та кількістю;
- бере профільні системи з товарів Perfex (`tblitems`);
- будує SVG-візуал конструкції;
- зберігає розрахунок і візуал до пропозиції;
- дозволяє виводити результат в шаблоні пропозиції через merge-field `{window_calculator_visual}`.

## Швидке встановлення

1. Скопіювати папку `modules/window_calculator` у ваш Perfex CRM.
2. Увімкнути модуль в адмінці Perfex.
3. Перейти в `Window Calculator` (лівий сайдбар).
4. У шаблоні пропозиції використати `{window_calculator_visual}`.

## Налаштування товарів (профілів)

Модуль бере активні товари з `tblitems`. Поля:

- `description` — назва профілю;
- `rate` — базова ціна за м²;
- `long_description` — optional JSON з коефіцієнтами:

```json
{
  "openingSurcharge": 850,
  "archFactor": 1.35,
  "triangleFactor": 1.4
}
```

Якщо JSON не заданий, застосовуються значення за замовчуванням.

## Оновлення у поточній версії

- Додано покрокову навігацію вкладками: **Форма → Секції → Перегляд → Пропозиція**.
- Додано редактор секцій (типи стулок) та відображення секцій у SVG-перегляді.
- Виправлено збереження в пропозицію з CSRF та повідомленнями `alert_float`.
- Виправлено інтеграцію merge-field `{window_calculator_visual}` для перегляду/друку пропозиції (повертається лише рядок для заміни).

## Діагностика, якщо щось не працює

1. Перевірте, що модуль активований у Perfex.
2. Переконайтеся, що в `tblitems` є товари (для профільних систем).
3. Після оновлення модуля очистіть кеш браузера та кеш Perfex.
4. Для друку/перегляду пропозиції перевірте, що у шаблоні використовується саме `{window_calculator_visual}`.


## Стан синхронізації репозиторію

- Оновлено версію модуля до `1.0.1` для фіксації актуальних змін у git.
- Якщо локально не видно змін, виконайте `git pull` у вашій копії репозиторію.
